"""Thin Shopify Admin GraphQL client for the photo lane.

The Python sibling of ``shop-photo-editor/lib/shopify.ts`` (the proven
staged-upload path against this store). Auth helpers are copied — not imported —
from ``japanese-invoice-transcriber`` so this package has no cross-project
dependency.
"""
