node_modules
.cache
data/*.db
data/*.db-journal
out
*.log
.DS_Store

# Secrets — match .env, .env.bak, .env.local, etc., but keep .env.example
.env
.env.*
!.env.example

# Python
__pycache__/
*.pyc
*.pyo
.pytest_cache/
*.egg-info/
.venv/
venv/

# Scraper / runtime state (contains session tokens, cookies, scraped HTML)
**/buyee/state/

# IDE
.vscode/
.idea/

# shop-photo-editor (Next.js app) — local-only runtime state
# `node_modules` already covered above (matches anywhere in tree)
.next/
shop-photo-editor/storage/      # 100MB+ of Gemini-generated JPGs
shop-photo-editor/data/*.db
shop-photo-editor/data/*.db-journal

# Raw camera-card source JPGs used during crop work — kept local only,
# they're large (>17 MB each) and not referenced by application code.
test-content/before/
